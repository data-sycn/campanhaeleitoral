import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.campanhaeleitoral',
  appName: 'campanhaeleitoral',
  webDir: 'dist'
};

export default config;
